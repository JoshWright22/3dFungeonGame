using Unity.Collections;
using UnityEngine;

public class RoomMaker : MonoBehaviour
{
    //Pick room type
    //Create floor and ceiling of type
    //Decide exits and create doors of type
    //Create walls of type
    //Place props, traps and enemies
    public int x;
    public int y;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
